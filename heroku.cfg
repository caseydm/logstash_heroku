input {
    stdin {}
}
filter {
    grok {
      match => {"message" => "%{TIMESTAMP_ISO8601:timestamp} %{WORD:component}\[%{WORD:process}\]: %{GREEDYDATA:syslog_message}" }
    }
    kv { source => "syslog_message" }
}
output {
    stdout{}
}